# dependencies (junctioned outside OneDrive — see CLAUDE.md)
node_modules/

# build output
dist/
.astro/

# large local media / harvest — NEVER commit (exceeds GitHub's 100 MB file limit; would break the push)
Videos/
fairytails-image-archive/

# breed-matcher tool — timestamped backups (see tools/breed-matcher/CLAUDE.md §1)
tools/breed-matcher/backups/*
!tools/breed-matcher/backups/.gitkeep

# dog-exercise-calculator tool — timestamped backups (see tools/dog-exercise-calculator/CLAUDE.md §1)
tools/dog-exercise-calculator/backups/*
!tools/dog-exercise-calculator/backups/.gitkeep

# misc
*.log
.DS_Store
Thumbs.db

# Owner-supplied master image pool (source files; processed copies go to src/assets)
Master images/

# SEO operating system — public repo: strategy/KPIs/competitor data stay out (OneDrive syncs it across machines)
SEO.md
